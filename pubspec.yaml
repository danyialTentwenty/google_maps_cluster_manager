name: google_maps_cluster_manager

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_maps_flutter_platform_interface:
    git:
      url: https://github.com/danyialTentwenty/plugins.git
      path: packages/google_maps_flutter/google_maps_flutter_platform_interface

dev_dependencies:
  flutter_test:
    sdk: flutter
